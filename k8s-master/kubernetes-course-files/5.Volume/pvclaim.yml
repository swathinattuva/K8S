apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: californiaapp-data
spec:
  storageClassName: managed-nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
